we need to have a way to upload a txt file which will be parsed in this manner, new lines become new rows. each row will have a question (prompt) and 1 or more answers. the question and answer delimiter is ~, and the delimiter between answers are ,  make sure you think of a robust solution for this and beautiful UI which is nested in the left side bar tab. this feature should be IN a lobby, not the home page