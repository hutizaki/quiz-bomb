# Static assets

Place **`typeSound.wav`** here to play a sound on each key stroke in the typing component.  
WAV is recommended for low latency; `typeSound.mp3` also works (update path in `src/constants/audio.ts` if you use MP3).
