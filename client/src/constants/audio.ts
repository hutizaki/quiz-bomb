/**
 * Path to typing key sound. Served from public/assets/.
 * WAV recommended (low latency); MP3 also fine. Place file in client/public/assets/.
 */
export const TYPE_SOUND_PATH = '/assets/typeSound.mp3'

/**
 * Explosion sound when bomb timer runs out (lose heart).
 * Place file at client/public/assets/explosion.mp3 (or use same path from your build output).
 */
export const EXPLOSION_SOUND_PATH = '/assets/explosion.mp3'
